Входные данные для модуля ANSWER_EVAL (math):

1) JSON-запрос в формате ANSWER_EVAL.request.math.v1:
```json
{{request_json}}
```

2) К этому сообщению прикреплено ИЗОБРАЖЕНИЕ с ответом ученика, соответствующее `answer_image_ref`.

Твоя задача:

1) Используя ТОЛЬКО `task_struct` и `raw_task_text`, понять:
   • что требуется по математике;
   • какой формат ответа ожидается (одно число, число с единицами, несколько подпунктов a/б/в, выражение и т.п.).

2) По фото:
   • аккуратно извлечь ответ (числа, знаки, единицы);
   • если есть графические элементы (координатная прямая, точки, стрелки) — учитывать их по изображению.

3) Сначала оценить качество фото:
   • если ответ неразборчив или виден не полностью — НЕ угадывать, вернуть:
     decision="cannot_evaluate", can_evaluate=false, failure_reason="bad_photo"
   • если ответа не видно — вернуть:
     decision="cannot_evaluate", can_evaluate=false, failure_reason="no_visible_answer"

4) Если проверка возможна:
   • нормализовать ответ (запятая/точка, дроби, знаки, единицы);
   • сначала независимо решите задачу, не учитывая ответ ученика и final_answer PARSE;
   • подтвердите independent_answer вторым способом для ЛЮБОГО типа задачи;
   • сравните independent_answer с PARSE: при расхождении верните invalid_expected/parse_inconsistency и не оценивайте ученика;
   • только при полном совпадении и verification_complete=true сравнивайте эталон с ответом ученика;
   • для визуальной задачи перечислите каждый проверенный объект в debug.visual_evidence;
   • если не удалось честно подтвердить — верните can_evaluate=false, decision="cannot_evaluate", failure_reason="ground_truth_unverified".
   • вернуть decision="correct" или decision="incorrect", can_evaluate=true.

5) feedback:
   • коротко и по делу, с учётом `student.grade`.
   • если неверно — укажите, что именно не совпало (например, «в пункте б)» или «не тот ответ»), и дайте 1 маленькую подсказку «что проверить».

Верни результат строго по схеме ANSWER_EVAL.response.math.v1 (только JSON, без лишнего текста).
